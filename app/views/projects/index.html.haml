%h2.title プロジェクト
- if @projects.empty?
  .empty
    %p プロジェクトありません。
- else
  %ul.projects.tasks_list
    = render :partial => 'project', :collection => @projects
  = will_paginate(@projects)
%ul.projects_buttons
  %li= link_to t('Create Project'), new_project_path
