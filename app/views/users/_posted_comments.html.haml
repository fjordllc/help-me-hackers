.content.posted_comments.tasks_list
  %h3 コメントしたタスク
  - if user.comments.empty?
    %p.empty= t('label.nothing_posted_comments')
  - else
    %ul.tasks
      - tasks = user.comments.correct.map {|c| c.task }
      = render :partial => 'tasks/object_small', :collection => tasks
