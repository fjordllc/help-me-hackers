.section.post
  - form_for task do |f|
    = f.error_messages
    %div.section
      %div.label
        = f.label :title
      = f.text_field :title
    %div.section
      %div.label
        = f.label :category, '', :for => 'task_category_id'
        %span== （#{link_to '詳細', about_category_path}）
      = select_by_model task, :category_id
    %div.section
      %div.label
        == #{f.label :tag_list}（","カンマ区切りで複数入力可能）
      = f.text_field :tag_list
    %div.section
      %div.label
        = f.label :language, '', :for => 'task_language_id'
      = select_by_model task, :language_id
    %div.section
      %div.label
        = f.label :license, '', :for => 'task_license_id'
        %span== （#{link_to '詳細', about_license_path}）
      = select_by_model task, :license_id
    %div.section
      %div.label
        = f.label :bounty
      = f.text_field :bounty
      = t('number.currency.format.unit')
    %div.section
      %div.label
        = f.label :description
        %span= "（#{link_to "Markdown記法", 'http://blog.2310.net/archives/6', :target => '_blank'} が使えます）"
      = f.text_area :description, :size => '70x10'
    %h4 プレビュー
    .wrapper
      .wmd-preview
    %p
      = f.submit '利用規約に同意して登録する'
      = link_to '利用規約', about_terms_path, :target => '_blank'
- content_for :footer_script do
  = javascript_include_tag 'wmd/wmd'
