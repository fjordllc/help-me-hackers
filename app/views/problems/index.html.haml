- add_title = params[:name] ? " - " + t("label.category.#{@category.name}") : ''
- add_class = params[:name] ? ' ' + @category.name : ''
%div{:class => "content#{add_class}"}
  %h2= "問題#{add_title}"
%ul.problems
  = render :partial => 'object', :collection => @problems
  = will_paginate(@problems)
